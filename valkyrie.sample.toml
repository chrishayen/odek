# Valkyrie project config
# Run `valkyrie init` to generate this file, or copy and edit manually.

project = "myapp"

[agent]
type = "claude-sub"
# model = "claude-sonnet-4-5"
# token = "sk-ant-oat01-..."       # explicit token (optional)
# token_env = "MY_TOKEN_VAR"       # env var name (optional)
# image = "ghcr.io/chrishayen/valkyrie-claude:latest"  # default
# By default, token is parsed from ~/.claude/.credentials.json
