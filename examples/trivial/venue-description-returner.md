# Requirement: "a library that returns an event venue description"

Gibberish input; best-effort interpretation is a single lookup returning a venue description string.

std: (all units exist)

venue
  venue.describe
    fn () -> string
    + returns a fixed venue description string
    ? the description is hardcoded; no parameters
    # lookup
