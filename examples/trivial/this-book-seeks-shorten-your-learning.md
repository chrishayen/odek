# Requirement: "a short motivational text returned as a string"

The input was a book's marketing blurb rather than a library. Best-effort interpretation: return the canonical motto as a string.

std: (all units exist)

motto
  motto.get
    @ () -> string
    + returns a short fixed motivational string
    ? text is hardcoded; no parameter
    # motto
