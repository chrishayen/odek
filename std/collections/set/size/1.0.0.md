---
version: 1.0.0
signature: '(s: set[T]) -> i64'
responsibility: set_query
---

# std.collections.set.size

Returns the number of elements in the set.

## Signature

(s: set[T]) -> i64

## Behavior

Returns the count of distinct elements. O(1).

## Tests

+ returns 0 for an empty set
+ returns the number of unique elements
- never returns a negative value

## Assumptions

? O(1)
