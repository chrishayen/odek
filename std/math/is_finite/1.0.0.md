---
version: 1.0.0
signature: '(x: f64) -> bool'
dependencies:
  - std.float.is_finite
responsibility: float_classification
---

# std.math.is_finite

Facade over `std.float.is_finite`.

## Signature

(x: f64) -> bool

## Behavior

Delegates to `std.float.is_finite`.

## Tests

+ returns true for any finite value including 0 and subnormals
+ returns false for ±infinity and NaN

## Assumptions

? facade; canonical impl lives in float/
