---
version: 1.0.0
signature: '(x: f64) -> bool'
dependencies:
  - std.float.is_infinite
responsibility: float_classification
---

# std.math.is_infinite

Facade over `std.float.is_infinite`.

## Signature

(x: f64) -> bool

## Behavior

Delegates to `std.float.is_infinite`.

## Tests

+ returns true for positive and negative infinity
+ returns false for NaN and all finite values

## Assumptions

? facade; canonical impl lives in float/
