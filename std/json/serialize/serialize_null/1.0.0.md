---
version: 1.0.0
signature: '() -> string'
responsibility: literal_serialization
---

# std.json.serialize.serialize_null

Returns the JSON null literal.

## Signature

() -> string

## Behavior

Returns the four-byte literal `null`.

## Tests

+ returns "null"
+ returns the same value on every call

## Assumptions

? the literal is lowercase and unquoted
