[agents.claude-pro]
type = "claude-pro"
model = "claude-sonnet-4-5"
# token = "sk-ant-oat01-..."  # OAuth token from `claude setup-token` (or set CLAUDE_CODE_OAUTH_TOKEN env var)

[agents.claude-api]
type = "claude-api"
model = "claude-sonnet-4-5"
api_key_env = "ANTHROPIC_API_KEY"

[agents.docker]
type = "docker"
image = "ubuntu:22.04"
