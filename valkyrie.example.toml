[agents.claude-pro]
type = "claude-pro"
model = "claude-sonnet-4-5"
# token_env = "CLAUDE_CODE_OAUTH_TOKEN"  # default — run `claude setup-token` to get this

[agents.claude-api]
type = "claude-api"
model = "claude-sonnet-4-5"
api_key_env = "ANTHROPIC_API_KEY"

[agents.docker]
type = "docker"
image = "ubuntu:22.04"
